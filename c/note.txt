
c语言
1.语言基础
	进制间转换
	编码(ascii / utf-8)
	c程序基本组成
		头文件
		main函数
	变量
		定义
		赋值
		初始化
		标识符
		关键字
	数据类型
	运算符	
	常量
2.控制语句
	选择语句
		if () {
		}
		if () {} else {}
		if () {} else if () {} else {}
		switch() {}
	循环语句
		for () {}
		while () {}
		do {} while ();
		continue / break
3.函数
	定义/声明/调用(传参时值传递过程)
	参数列表
	返回值(int)
	变量类型:
		局部变量
		全局变量
		块变量	
	修饰变量的关键字
		static
		const
		register
		volatile
		auto
		extern
	c存储空间布局
		栈
		bss段
		data段
		堆
	递归函数
	变参函数

	宏

4.数组
	定义/赋值/初始化
	数组名含义?
	数组名类型?
	字符数组和字符串
		strlen(3)
		strcpy(3) / strncpy(3)
		strcat(3) / strncat(3)
		strcmp(3)
	二维数组
	排序
		冒泡/选择/直接插入
	
5.指针
	定义/赋值/初始化	
	意义
	指针类型的作用	
	* / []
	二级指针
	数组指针
	函数指针
	指针数组
	NULL(空指针) / void *(万能指针)

6. 结构体
	struct 
	存储空间大小
	成员偏移量
	大小端问题
	结构体类型数组
	结构体类型指针
	union
	enum










